<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{{ category }} - Peak Interval Blog</title>
    
    <!-- SEO Meta Tags -->
    <meta name="description" content="Read the latest {{ category }} articles on the Peak Interval Blog.">
    <meta name="robots" content="index, follow">
    <link rel="canonical" href="https://peakintervalapp.com/blog/{{ category | slugify }}/">

    <!-- Favicon -->
    <link rel="icon" type="image/x-icon" href="/favicon.ico">

    <!-- Open Graph Meta Tags -->
    <meta property="og:title" content="{{ category }} - Peak Interval Blog">
    <meta property="og:description" content="Read the latest {{ category }} articles on the Peak Interval Blog.">
    <meta property="og:image" content="https://peakintervalapp.com/assets/app_icon.png">
    <meta property="og:url" content="https://peakintervalapp.com/blog/{{ category | slugify }}/">
    <meta property="og:type" content="website">

    <!-- Twitter Card Meta Tags -->
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:title" content="{{ category }} - Peak Interval Blog">
    <meta name="twitter:description" content="Read the latest {{ category }} articles on the Peak Interval Blog.">
    <meta name="twitter:image" content="https://peakintervalapp.com/assets/app_icon.png">

    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, sans-serif;
            background: #181F3B;
            color: white;
            padding: 0;
            margin: 0;
            min-height: 100vh;
        }
        
        .header {
            padding: 20px;
            text-align: center;
            background-color: rgba(0,0,0,0.2);
        }
        
        .logo {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 15px;
        }
        
        .app-icon {
            width: 60px;
            height: 60px;
            border-radius: 15px;
        }
        
        .logo-text {
            font-size: 24px;
            font-weight: 700;
        }
        
        .nav {
            display: flex;
            justify-content: center;
            gap: 30px;
            padding: 15px 0;
            background-color: rgba(0,0,0,0.3);
        }
        
        .nav a {
            color: white;
            text-decoration: none;
            font-weight: 500;
            opacity: 0.8;
            transition: opacity 0.2s;
        }
        
        .nav a:hover {
            opacity: 1;
        }
        
        .nav a.active {
            opacity: 1;
            border-bottom: 2px solid #FF9500;
        }
        
        .category-header {
            text-align: center;
            padding: 60px 20px 40px;
        }
        
        .category-title {
            font-size: 42px;
            margin-bottom: 20px;
        }
        
        .category-description {
            font-size: 20px;
            max-width: 700px;
            margin: 0 auto 40px;
            line-height: 1.5;
            opacity: 0.9;
        }
        
        .blog-content {
            max-width: 1000px;
            margin: 0 auto;
            padding: 0 20px 60px;
        }
        
        .back-link {
            display: inline-block;
            margin-bottom: 30px;
            color: #FF9500;
            text-decoration: none;
            font-weight: 500;
        }
        
        .back-link:hover {
            text-decoration: underline;
        }
        
        .blog-posts {
            display: grid;
            grid-template-columns: repeat(1, 1fr);
            gap: 20px;
        }
        
        @media (min-width: 768px) {
            .blog-posts {
                grid-template-columns: repeat(2, 1fr);
            }
        }
        
        .blog-post-card {
            background-color: rgba(0,0,0,0.2);
            border-radius: 15px;
            overflow: hidden;
        }
        
        .post-link {
            text-decoration: none;
            color: white;
            display: flex;
            flex-direction: column;
            height: 100%;
        }
        
        .post-thumbnail {
            width: 100%;
            height: 200px;
            object-fit: cover;
        }
        
        .post-thumbnail-placeholder {
            height: 200px;
            background-color: rgba(255,255,255,0.1);
        }
        
        .post-details {
            padding: 20px;
        }
        
        .post-title {
            font-size: 24px;
            margin-bottom: 10px;
        }
        
        .post-meta {
            font-size: 14px;
            color: rgba(255,255,255,0.7);
            margin-bottom: 10px;
        }
        
        .post-excerpt {
            margin-bottom: 20px;
        }
        
        .read-more {
            color: #FF9500;
            text-decoration: none;
            font-weight: 600;
            transition: opacity 0.2s;
        }
        
        .read-more:hover {
            opacity: 0.7;
        }
        
        .footer {
            padding: 40px 20px;
            text-align: center;
        }
        
        .footer-links {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin-bottom: 20px;
        }
        
        .footer-links a {
            color: white;
            text-decoration: none;
            opacity: 0.7;
            transition: opacity 0.2s;
        }
        
        .footer-links a:hover {
            opacity: 1;
            text-decoration: underline;
        }
        
        .copyright {
            opacity: 0.7;
            font-size: 14px;
        }
    </style>
</head>
<body>
    <div class="header">
        <div class="logo">
            <a href="/">
                <img src="/assets/app_icon.png" alt="Peak Interval App Icon" class="app-icon">
            </a>
            <div class="logo-text">Peak Interval</div>
        </div>
    </div>
    
    <div class="nav">
        <a href="/">Home</a>
        <a href="/blog/" class="active">Blog</a>
        <a href="/contact/">Contact</a>
    </div>
    
    <div class="category-header">
        <h1 class="category-title">{{ category }}</h1>
        <p class="category-description">
            {% if category == "HIIT Fundamentals" %}
                Learn the basics and core principles of High-Intensity Interval Training.
            {% elsif category == "Workout Guides" %}
                Explore effective HIIT workout routines designed for different fitness levels and goals.
            {% elsif category == "App Features" %}
                Discover how to get the most out of the Peak Interval app with these feature guides.
            {% elsif category == "Specialized HIIT Training" %}
                HIIT workouts tailored for specific activities, sports, and special populations.
            {% elsif category == "Fitness + Nutrition" %}
                Nutrition strategies and fitness tips to maximize your HIIT training results.
            {% elsif category == "HIIT Success Stories" %}
                Real-life transformations and success stories from HIIT enthusiasts.
            {% elsif category == "Advanced HIIT Concepts" %}
                Advanced training methodologies and scientific concepts behind HIIT.
            {% else %}
                Browse our collection of articles about {{ category }}.
            {% endif %}
        </p>
    </div>
    
    <div class="blog-content">
        <a href="/blog/" class="back-link">← Back to Categories</a>
        
        <div class="blog-posts">
            {%- assign posts = collections.posts | reverse -%}
            {%- for post in posts -%}
                {% if post.data.category == category %}
                <div class="blog-post-card">
                    <a href="{{ post.url | url }}" class="post-link">
                        {% if post.data.featured_image %}
                        <img src="{{ post.data.featured_image | url }}" alt="{{ post.data.image_alt | default: post.data.title }}" class="post-thumbnail">
                        {% else %}
                        <div class="post-thumbnail-placeholder"></div>
                        {% endif %}
                        <div class="post-details">
                            <h2 class="post-title">{{ post.data.title }}</h2>
                            {% if post.date %}
                            <div class="post-meta">Published on {{ post.date | date: "%B %d, %Y" }}</div>
                            {% endif %}
                            <p class="post-excerpt">
                                {{ post.data.description }}
                            </p>
                            <span class="read-more">Read More →</span>
                        </div>
                    </a>
                </div>
                {% endif %}
            {%- endfor -%}
        </div>
    </div>
    
    <div class="footer">
        <div class="footer-links">
            <a href="/">Home</a>
            <a href="/terms/">Terms & Conditions</a>
            <a href="/privacy/">Privacy Policy</a>
            <a href="/contact/">Contact Us</a>
            <a href="/blog/">Blog</a>
        </div>
        <div class="copyright">
            &copy; 2023-2024 Peak Interval. All rights reserved.
        </div>
    </div>

    <!-- Firebase Analytics -->
    <script type="module">
        // Import the functions you need from the SDKs you need
        import { initializeApp } from "https://www.gstatic.com/firebasejs/10.12.2/firebase-app.js";
        import { getAnalytics } from "https://www.gstatic.com/firebasejs/10.12.2/firebase-analytics.js";
        // TODO: Add SDKs for Firebase products that you want to use
        // https://firebase.google.com/docs/web/setup#available-libraries

        // Your web app's Firebase configuration
        // For Firebase JS SDK v7.20.0 and later, measurementId is optional
        const firebaseConfig = {
            apiKey: "AIzaSyAH8e8l2Q7f-1ZXYYeDrMfB4oKGKEwoCxM",
            authDomain: "peak-interval-site.firebaseapp.com",
            projectId: "peak-interval-site",
            storageBucket: "peak-interval-site.firebasestorage.app",
            messagingSenderId: "199931231891",
            appId: "1:199931231891:web:97e2dd591118fdcaf29766",
            measurementId: "G-TGBMET8NFR"
        };

        // Initialize Firebase
        const app = initializeApp(firebaseConfig);
        const analytics = getAnalytics(app);
    </script>
</body>
</html> 